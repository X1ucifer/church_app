/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
        ignoreDuringBuilds: true,
    },
    // output: 'export',
    // images: {
    //     loader: 'custom',
    // },
    // trailingSlash: true
};

export default nextConfig;
